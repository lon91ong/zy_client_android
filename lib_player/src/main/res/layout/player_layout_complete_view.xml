<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/complete_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#22000000">

    <ImageView
        android:id="@+id/stop_fullscreen"
        android:layout_width="@dimen/dimen_player_controller_height"
        android:layout_height="@dimen/dimen_player_controller_height"
        android:padding="@dimen/dimen_player_controller_icon_padding"
        android:src="@drawable/ic_player_arrow_back"
        android:visibility="gone"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_replay"
            android:layout_width="@dimen/dimen_player_play_btn_size"
            android:layout_height="@dimen/dimen_player_play_btn_size"
            android:layout_gravity="center"
            android:background="@drawable/player_shape_play_bg"
            android:padding="@dimen/dimen_player_controller_icon_padding"
            android:src="@drawable/ic_player_replay" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/str_player_replay"
            android:textColor="@android:color/white" />

    </LinearLayout>
</FrameLayout>